node_modules/
generated/
build/
# Each manifest builds to its own directory so they don't overwrite each
# other's output.
build-pool/
build-robinhood/
# Matchstick binary, fetched per-platform. 27MB and not source.
.bin/
# Matchstick writes the version it last ran into the tests folder.
tests/.latest.json
